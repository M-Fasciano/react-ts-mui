import styled from "styled-components";

export const StyledNav = styled("nav")`
  padding: 2rem;

  .list {
    display: flex;
    gap: 10px;
    margin: 0;
    padding: 0;
  }

  .list__item {
    list-style: none;
  }
`;
